

return {
    client = {
        key = require("config.bindings.client.key"),
        mouse = require("config.bindings.client.mouse"),
    },
    global = {
        key = require("config.bindings.global.key"),
        mouse = require("config.bindings.global.mouse"),
    },
    mod = require("config.bindings.mod")
}
