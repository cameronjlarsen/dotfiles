return function(config)
    -- Default term type
    config.term = "wezterm"

    -- General settings
    config.automatically_reload_config = true
    config.audible_bell = "Disabled"

    return config
end
